use science
use ml

precision(80)
print("sqrt(2) =", sqrt(2))
print("dot =", science.dot([1.0, 2.0], [3.0, 4.0]))

let model = ml.linear_regression([1.0, 2.0, 3.0], [2.0, 4.0, 6.0])
print("prediction =", ml.predict(model, 4.0))
