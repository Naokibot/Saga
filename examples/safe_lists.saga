let original = [10, 20, 30]
let added = append(original, 40)

print("original:", original)
print("added:", added)

# 範囲外でも停止させたくない場合は、getの既定値を使います。
print(get(original, 99, -1))

for value in added {
    if value == 20 {
        continue
    }
    print(value)
}
